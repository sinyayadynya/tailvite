{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}
<!-- <input{{ attributes.addClass('focus:ring-indigo-500 focus:border-indigo-500 block sm:text-sm border-gray-300 rounded-md') }} />{{ children }} -->

<input{{ attributes.addClass('form-item__textfield') }} />{{ children }}
