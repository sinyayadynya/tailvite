{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{# Additional style in 01-atoms/buttons/buttons.scss #}

<input{{ attributes.addClass('cursor-pointer inline-flex items-center py-3 px-4 border text-sm font-medium leading-4 font-medium rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2') }} />
    {{ children }}
